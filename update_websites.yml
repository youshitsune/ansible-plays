---
- name: Update websites
  hosts: home
  vars_prompt:
    - name: pass
      prompt: Type in your password
      private: true

  tasks:
    - name: Update HTTP/HTTPS website
      shell: echo {{pass|quote}} | sudo -S systemctl restart kineto

    - name: Update Gemini capsule
      shell: cd gemini/content/ && git pull
